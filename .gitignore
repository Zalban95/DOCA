node_modules/
.env
.certs/
.dashboard-prefs.json
.doca/

# Runtime artefacts that only appear here when a data path resolves against the
# working directory (a bug, fixed) — never part of the repo.
/agents/
/harness/

# Local harness transcripts and dashboard debug output.
/.aider.chat.history.md
/.aider.input.history
/dashboard.log
/debug-*.log
/.releases/
